1. Used `this..getters.roles` to check user roles in Vue components.
2. Used `v-if` to conditionally render columns and form items based on user roles.
3. Replaced sensitive data with `***` when hiding it in tables and descriptions.
