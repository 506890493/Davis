<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.CmsNotificationMapper">

    <resultMap type="com.ruoyi.system.domain.CmsNotification" id="CmsNotificationResult">
        <result property="notificationId" column="notification_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="notificationType" column="notification_type"/>
        <result property="relatedId" column="related_id"/>
        <result property="isRead" column="is_read"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.ruoyi.system.domain.CmsNotification" useGeneratedKeys="true" keyProperty="notificationId">
        INSERT INTO cms_notification(user_id, title, content, notification_type, related_id, is_read, create_time)
        VALUES(#{userId}, #{title}, #{content}, #{notificationType}, #{relatedId}, '0', NOW())
    </insert>

    <select id="selectByUserId" resultMap="CmsNotificationResult">
        SELECT * FROM cms_notification WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT 50
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM cms_notification WHERE user_id = #{userId} AND is_read = '0'
    </select>

    <update id="markRead">
        UPDATE cms_notification SET is_read = '1' WHERE notification_id = #{notificationId}
    </update>

    <update id="markAllRead">
        UPDATE cms_notification SET is_read = '1' WHERE user_id = #{userId}
    </update>

    <select id="existsByUserAndRelated" resultType="int">
        SELECT COUNT(*) FROM cms_notification
        WHERE user_id = #{param1} AND related_id = #{param2} AND notification_type = #{param3}
        AND is_read = '0'
    </select>

</mapper>
