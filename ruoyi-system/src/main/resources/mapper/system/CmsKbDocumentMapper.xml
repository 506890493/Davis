<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.CmsKbDocumentMapper">

    <resultMap type="CmsKbDocument" id="BaseResultMap">
        <id     property="id"             column="id"/>
        <result property="categoryId"     column="category_id"/>
        <result property="title"          column="title"/>
        <result property="docType"        column="doc_type"/>
        <result property="summary"        column="summary"/>
        <result property="tags"           column="tags"/>
        <result property="coverImageId"   column="cover_image_id"/>
        <result property="primaryFileId"  column="primary_file_id"/>
        <result property="isRequired"     column="is_required"/>
        <result property="status"         column="status"/>
        <result property="publishedTime"  column="published_time"/>
        <result property="viewCount"      column="view_count"/>
        <result property="currentVersion" column="current_version"/>
        <result property="delFlag"        column="del_flag"/>
        <result property="deleteTime"     column="delete_time"/>
        <result property="createBy"       column="create_by"/>
        <result property="createTime"     column="create_time"/>
        <result property="updateBy"       column="update_by"/>
        <result property="updateTime"     column="update_time"/>
        <result property="remark"         column="remark"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, category_id, title, doc_type, summary, tags, cover_image_id, primary_file_id,
        is_required, status, published_time, view_count, current_version, del_flag, delete_time,
        create_by, create_time, update_by, update_time, remark
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM cms_kb_document
        WHERE id = #{id} AND del_flag = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap" parameterType="CmsKbDocument">
        SELECT <include refid="Base_Column_List"/>
        FROM cms_kb_document
        <where>
            del_flag = 0
            <if test="categoryId != null">AND category_id = #{categoryId}</if>
            <if test="title != null and title != ''">AND title LIKE CONCAT('%', #{title}, '%')</if>
            <if test="docType != null and docType != ''">AND doc_type = #{docType}</if>
            <if test="status != null">AND status = #{status}</if>
            <if test="isRequired != null">AND is_required = #{isRequired}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="CmsKbDocument" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cms_kb_document(category_id, title, doc_type, summary, tags,
                                    cover_image_id, primary_file_id, is_required, status,
                                    published_time, view_count, current_version, del_flag,
                                    create_by, create_time, update_by, update_time, remark)
        VALUES(#{categoryId}, #{title}, #{docType}, #{summary}, #{tags},
               #{coverImageId}, #{primaryFileId}, #{isRequired}, #{status},
               #{publishedTime}, #{viewCount}, #{currentVersion}, #{delFlag},
               #{createBy}, sysdate(), #{updateBy}, sysdate(), #{remark})
    </insert>

    <update id="update" parameterType="CmsKbDocument">
        UPDATE cms_kb_document
        <set>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="docType != null">doc_type = #{docType},</if>
            <if test="summary != null">summary = #{summary},</if>
            <if test="tags != null">tags = #{tags},</if>
            <if test="coverImageId != null">cover_image_id = #{coverImageId},</if>
            <if test="primaryFileId != null">primary_file_id = #{primaryFileId},</if>
            <if test="isRequired != null">is_required = #{isRequired},</if>
            <if test="status != null">status = #{status},</if>
            <if test="publishedTime != null">published_time = #{publishedTime},</if>
            <if test="viewCount != null">view_count = #{viewCount},</if>
            <if test="currentVersion != null">current_version = #{currentVersion},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_by = #{updateBy},
            update_time = sysdate()
        </set>
        WHERE id = #{id} AND del_flag = 0
    </update>

    <update id="updateStatus">
        UPDATE cms_kb_document
        SET status = #{status},
            published_time = #{publishedTime},
            update_time = sysdate()
        WHERE id = #{id} AND del_flag = 0
    </update>

    <update id="updateCurrentVersion">
        UPDATE cms_kb_document
        SET current_version = #{currentVersion},
            update_time = sysdate()
        WHERE id = #{id} AND del_flag = 0
    </update>

    <update id="softDelete">
        UPDATE cms_kb_document
        SET del_flag = 1, delete_time = #{deleteTime}, update_time = sysdate()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="hardDelete">
        DELETE FROM cms_kb_document
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="countByCategory" resultType="int">
        SELECT COUNT(1) FROM cms_kb_document
        WHERE category_id = #{categoryId} AND del_flag = 0
    </select>

    <select id="countExpiredInRecycle" resultType="int">
        SELECT COUNT(1) FROM cms_kb_document
        WHERE del_flag = 1 AND delete_time &lt; #{cutoff}
    </select>
</mapper>
